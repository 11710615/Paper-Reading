# 量化

## 1. AWQ量化：
### 定义：
AWQ 的全称是 Activation-aware Weight Quantization，核心是「激活感知」—— 它不是盲目量化权重，而是根据输入激活值的分布，找到对模型输出影响最大的权重，对其进行保护或最优缩放，从而在 INT4 低比特下保持精度。
AWQ 的完整量化流程（必须用到校准集）
* 准备校准集：收集 128~512 个和真实推理场景分布一致的样本（比如几百条文本、几张图片），不需要标注；
* 前向传播统计激活值：把校准集输入到 FP16 原生模型，进行前向传播，统计每一层的激活值分布；
* 寻找最优缩放因子（AWQ 核心）：根据激活值分布，计算每一层的「重要权重掩码」和「最优缩放因子」，保护对输出影响大的权重；
* 执行权重量化：用找到的最优参数，把 FP16 权重量化为 INT4；
* 保存量化模型：保存 INT4 权重 + 缩放因子，生成 AWQ 量化模型。
### 问题：
#### 1. 这个缩放因子是用于int4*int4计算后，超出int4范围，用这个scale重新映射回int4，是吗
你理解的「缩放因子用于 int4*int4 计算后，超出范围重新映射回 int4」，是**全整型量化(W4A4：权重 int4 + 激活 int4)** 的逻辑，完全不符合你当前用的 AWQ 场景。
你正在用的 vLLM AWQ Marlin，是行业默认的 W4A16 仅权重量化：
* 权重：int4 格式存储，仅用来节省显存
* 激活值：全程保持 FP16 精度，不会量化成 int4
* 计算过程：根本不存在 int4*int4 的整数计算，自然也没有「计算后用 scale 映射回 int4」的步骤

#### 2. 什么是激活值，为什么用权重和通道平均激活值的乘积表明通道重要性？
##### 2.1. 预训练+微调的两阶段训练，天然导致权重和实际场景的错位
现在的大模型，都是「通用预训练+下游任务微调」的两阶段模式，这是权重和激活错位的最大来源：
- **预训练阶段**：模型在万亿级的通用文本上训练，覆盖中文、英文、代码、数学、诗歌、法律等所有场景。梯度下降会给「预训练数据里高频出现的场景」分配大权重——比如英文数据占比高，处理英文语法的通道，训练时激活值拉满，梯度更新频繁，最终学到的权重非常大。
- **微调/推理阶段**：你用模型的时候，只会用到它的一部分能力——比如你之前的日志里，用模型做人体关键点检测的多模态任务，几乎不会用到英文语法、诗歌押韵、数学公式这些能力。
这就会出现一个必然结果：
预训练里学到的大权重通道，在你的专属场景里，激活值永远接近0，对你的任务完全没用；反而一些预训练里权重不大，但在你的场景里激活值拉满的通道，才是真正的核心通道。
###### 大白话类比
你请了一个全能私人助理，给他开月薪5万（权重大），因为他会英语、法语、日语、开车、做饭、修电脑，是全行业的顶级人才（预训练通用能力强）。但你实际用他的时候，只在国内生活，只用他帮你处理中文的日常工作，从来不用他说外语、修电脑。
- 他的「英语能力」对应的权重（工资）很高，但你实际使用时的激活值（使用频率）几乎为0，对你来说完全不重要；
- 他的「中文文档处理」对应的权重（工资）不高，但激活值拉满，才是对你真正重要的能力。
##### 2.2. 权重的重要性，永远是「任务相关」的，没有绝对的重要性
梯度下降学到的权重大小，只代表「它在预训练的通用任务分布里，对降低损失的平均贡献」，**不代表它在所有任务里都重要**。
- 比如模型里有一个处理「数学公式符号」的通道，预训练时数学数据很多，它的激活值拉满，学到的权重很大，对预训练任务非常重要；
- 但你用模型来写小说、做日常对话，这个通道的激活值永远接近0，对你的任务来说，它完全没用，哪怕权重很大。
###总结
AWQ的校准集，本质就是用和你真实推理场景一致的样本，跑一遍模型，统计出哪些通道是对你的任务真正重要的，然后用最优缩放因子保护它们，牺牲无效的冗余通道

---

| Syntax      | Description | Test Text     |
| :---        |    :----:   |          ---: |
| Header      | Title       | Here's this   |
| Paragraph   | Text        | And more      |

***
